using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BoidParamModifier : MonoBehaviour
{
    // Start is called before the first frame update

    [SerializeField] private BoidLeader _leader;

    [SerializeField] private ModifierActor _actor;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
